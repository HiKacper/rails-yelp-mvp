<h2><strong>All Restaurants</strong></h2>
<%= link_to "Add new restaurant", new_restaurant_path(@restaurant), class: "btn btn-primary"%>

<ul class="list-group">
  <% @restaurants.each do |restaurant| %>
    <li class="list-group-item"><%= link_to restaurant.name, restaurant_path(restaurant) %></li>
  <% end %>
</ul>
<br>
